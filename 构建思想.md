### 1、Pimple思想

.h

```cpp
private:
    struct Impl;
    std::unique_ptr<Impl> impl_;
};
```

.cpp

```cpp
struct PortManager::Impl {
    std::array<PortChannel, kSlotCount> channels;
    std::mutex lock;
......
}
```

这样可以隐藏实现细节，降低头文件依赖，避免内部实现变化导致大量源文件重新编译。

### 2、mutex设计

全局锁 :

全局锁主要是为了保护 `channels` 里面的运行状态，防止轮询线程、IPC 命令线程、连接断开、添加删除设备这些流程同时读写，导致设备列表迭代器失效、端口状态不一致，或者 `bus` 被释放后仍然被访问。

```c++
//全局锁channels / devices / connected / last_data
struct PortManager::Impl {
    std::array<PortChannel, kSlotCount> channels;
    std::mutex lock;

.......
}
```

端口锁:

是为了保护 Modbus/RS485 通信。Modbus RTU 是主从一问一答，RS485 通常是半双工，同一端口下多个从站共享同一条总线。如果轮询线程正在读温湿度，同时 UI 命令又去写继电器，就可能出现帧冲突、响应错乱、超时或者 CRC 错误。所以同一端口的读写必须串行化。

```c++
//端口锁 ModbusMaster / RS485 / read / write
struct PortChannel {
    bool connected = false;
    std::string port;
    int baud = 0;
    std::unique_ptr<ModbusMaster> bus;
    std::vector<ManagedDevice> devices;
    bool poll_running = false;
    bool stop_requested = false;
    std::thread poll_thread;
    std::mutex io_lock;
};
```

### 3、unique_ptr和shard_ptr运用

```cpp
std::unique_ptr<Impl> impl_;
// impl_ 是 PImpl 设计，PortManager 用 unique_ptr 独占内部实现，隐藏端口数组、线程、锁、设备列表等细节，降低头文件耦合。
std::unique_ptr<ModbusMaster> bus;
// bus 用 unique_ptr 是因为 ModbusMaster 是端口独占资源，一个端口只有一个 bus。连接成功后把所有权转移给 channel.bus，断开端口时 reset 自动释放资源。
std::shared_ptr<SensorDevice> sensor;
// sensor 用 shared_ptr 是因为设备对象会被设备列表和轮询流程共同引用。轮询线程会先把要轮询的设备复制到due_devices，释放全局锁后再执行 Modbus 读取。shared_ptr 能保证即使设备从 channel.devices 删除，当前这次轮询手里的设备对象也不会提前析构，避免悬空指针。
```

